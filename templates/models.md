# Optional. Copy to `.agents/models.md` and uncomment a role to pin a host-native model.
# Delete the copy, or leave every role commented, to inherit the current chat model.
# `inherit` or `auto` means omit model (parent chat model).
# A comma-separated list means one subagent per entry.

# feature, refactoring: inherit
# bug-fix: inherit
# perf-issue: inherit
# hillclimb: inherit
# judgment and prose: inherit
# hardest tasks: inherit
# how explorer: inherit
# how explainer: inherit
# why investigators: inherit
# why synthesizer: inherit
# arena runners: inherit, inherit, inherit
# arena cross-judge pool: inherit
# swarm workers: inherit
# architect runners: inherit, inherit, inherit
# interrogate reviewers: inherit, inherit
